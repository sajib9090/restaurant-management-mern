import createError from "http-errors";
import Staff from "../models/staff.model.js";

const handleCreateStaff = async(req, res, next) => {
    try {
        
    } catch (error) {
        next(error);
    }
}

export {handleCreateStaff}